import {StyleSheet} from 'react-native';
import {colors} from '../../../../../config/app_config';

export const filterStyles = StyleSheet.create({
  textInput: {backgroundColor: colors.fadeBlack},
});
